For installation instructions, see INSTALL.

This package provides an implementation for
asynchronous network I/O for multiple languages.
Only C and Java are supported at the moment, and
C#/Python interfaces are planned for 0.2.
